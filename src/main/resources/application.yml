spring:
  application:
    name: server
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  docker:
    compose:
      enabled: false

  openai:
    api-key: ${OPENAI_API_KEY}
    model: gpt-4

  prompts:
      deep-prompt: |
      

      quick-prompt: |
      
