spring:
  application:
    name: server
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
  docker:
    compose:
      enabled: false

  openai:
    api-key: ${OPENAI_API_KEY}
    model: gpt-4

  prompts:
      deep-prompt: |
      

      quick-prompt: |
      
logging:
  level:
    com.safe_jeonse.server.service.PdfParseService: DEBUG